 your job is to make a system like skill, claude skill, antigravity skill it will do this follow thing: will see the
  parameter in the function then call this function? what will be the output now. these output will be answered in htlm
  box, all answer should store like later if i give an ai command that is another skill will automatically evaluate my
  answers. also some task may be in just project directory. not output task, you need to evaluate this by reading
  project code. a


• I’ll inspect the current workspace first so the implementation fits whatever project structure is already present.
  Then I’ll build the interview/task flow, answer storage, and project-code evaluation entry points around the existing
  stack.

• Ran Get-ChildItem -Force; rg --files -g '!node_modules' -g '!target' | Select-Object -First 200
  └

› your job is to make skills, not the interview coding task now


• Understood. I’ll focus on creating the reusable skill itself, not the interview UI or task engine. I’m going to read
  the local skill-authoring instructions and inspect the existing project for conventions, then scaffold the skill in
  the appropriate location.


• Ran 2 commands · ctrl + t to view transcript

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• The workspace is a Spring Boot project with a coding_qna_practice directory, but it has no existing Codex skill
  definitions. I’m treating your request as two reusable skills: one for discovering a project and producing/storing
  interview tasks, and a second for evaluating saved answers or code-only assignments later. I’ll keep them instruction-
  focused so they can be invoked by future AI commands.

• Ran Get-ChildItem -Force coding_qna_practice; Get-ChildItem -Recurse -File coding_qna_practice | Select-Object
  │ FullName,Length; Get-Content -Raw README.md; Get-Content -Raw coding_qna_practice/* -ErrorAction SilentlyContinue
  └ # E-Commerce API

    Spring Boot project for E-Commerce API.


• Ran 4 commands · ctrl + t to view transcript

────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────

• The skill skeletons are in place. I’m now replacing the generated placeholders with concrete operating rules:
  workspace discovery, optional lessons, HTML/session artifacts, durable answer records, and a separate evaluator that
  can grade both written answers and repository-only coding tasks.

• Edited 6 files (+133 -22)
  └ ~\.codex\skills\project-answer-evaluator\SKILL.md (+28 -8)
     3 -description: "[TODO: Briefly describe what this skill does and when it applies.]"
     4 ----
     5 -
     6 -# Project Answer Evaluator
     7 -
     8 -[TODO: Add the task-specific guidance Codex needs. Reference supporting files only when they are relevant.]
     1 +---
     2 +name: project-answer-evaluator
     3 +description: "Evaluate saved project interview answers and repository-only coding tasks against source evidence,
         tests, and a session rubric. Use when a user asks to grade, review, or give feedback on a session created by pr

  └ ~\.codex\skills\project-answer-evaluator\agents\openai.yaml (+4 -3)

  └ ~\.codex\skills\project-answer-evaluator\references\evaluation-rubric.md (+28 -0)

  └ ~\.codex\skills\project-interview-author\SKILL.md (+30 -8)

  └ ~\.codex\skills\project-interview-author\agents\openai.yaml (+4 -3)

  └ ~\.codex\skills\project-interview-author\references\session-schema.md (+39 -0)
  … Diff preview limited (ctrl + t to view transcript).