# Override with ignored config/application-mysqltest.properties on a developer machine.
spring.datasource.url=${TEST_DB_URL:jdbc:mysql://localhost:3306/atelier_store_test?serverTimezone=UTC}
spring.datasource.username=${TEST_DB_USERNAME:atelier_test}
spring.datasource.password=${TEST_DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
jwt.secret=test-only-signing-key-at-least-32-characters-long
