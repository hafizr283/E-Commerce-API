# Docker Compose only. Use unique values and never commit .env.
MYSQL_PASSWORD=replace-with-unique-app-password
MYSQL_ROOT_PASSWORD=replace-with-unique-root-password
JWT_SECRET=replace-with-random-secret-of-at-least-32-bytes
# Set true only for local portfolio demonstrations with the documented demo users.
APP_SEED_DEMO=false
